# 更新日志

| 日期 | 更新内容 |
|------|---------|
| 2026-06-12（v9） | **Skill架构规范v3.0合规整改**：① SKILL.md瘦身到≤500行（前置条件/企微操作/品牌调性/更新日志外迁到references/）；② 新增`tests/eval_set.md`评估集（P0强制）；③ YAML补`dependencies`字段；④ 步骤3a/4b重构熔断器为规范格式；⑤ 步骤3b补📦产出物锁 |
| 2026-06-12（v8） | **改文案方向偏离修复**：① 新增步骤3a.5「文案约束确认」（出修改建议前先确认核心保留词/参考案例/字数要求，防止AI脑补方向偏离）；② 术语统一："擦边度"→"可暗示度"（全文替换）；③ 尺度继承：步骤3b自动继承步骤1的审核尺度，不再重复询问；④ 修复企微写入逻辑（`edit_doc_content` 当前版本同步返回，去掉错误轮询，写入改判 `errcode=0` 即成功） |
| 2026-06-09（v7） | **架构合规审计整改**：① YAML补全（compatibility/allowed-tools/version/排除场景）；② 删除硬编码绝对路径（LLM02）；③ 新增⚠️铁律声明、触发条件（含排除场景）、防跳步机制汇总、可逆性分级🟢🟡🔴、禁止行为表、与其他Skill的关系、行动倾向；④ 步骤2后新增🧠思考检查点；⑤ 步骤4输出前新增🔒完整性锁；⑥ 新增v7更新日志 |
| 2026-05-08 | 首次创建；加入客户已法务背书词汇清单；加入打假风险判断框架；三步自检法；日化品类特有禁忌 |
| 2026-05-09 | 整合完整5步审核流程；加入企标标签对照环节（仅包装类型触发）；企标品类名与产品名不需完全一致规则；更新输出格式；优化步骤指引；加入确认提问环节（先问清楚再审）；加入输入双重自检逻辑（两种方式读+对比一致再审，OCR不可用改由多模态兜底）；修复企标章节号写死问题（改为通读全文自动定位） |
| 2026-05-25 | 双重自检改为逐字对比（删除≥80%相似度放行逻辑）；法务背书词汇在报告中列出+注明依据；PDF读取报错自动转图片兜底；输出模板产品名改为[产品名称]占位符；企标有效期检查（过期/即将过期提醒）；渠道分类细化（增加子类+高风险提醒）；IMA知识库接入（法务词库动态读取）；修复文件保存路径（必须保存到当前workspace目录）；**IMA读取失败不回退，改为解决问题**；**添加审核后自动录入功能**（确认可用词汇自动询问是否录入IMA） |
| 2026-05-25（v2） | **新增关键数据确认环节**（双重自检通过后、审核前必须执行）：提取规格/片数/成分/产品类型/执行标准/厂商等关键数据点，逐条报审核人确认后再进入审核。解决AI视觉识别数字混淆（如6↔5）导致审核结果全部作废的问题。修改通过标准：确认环节纳入必经流程 |
| 2026-05-25（v3） | **流程重构为四步制**：步骤1定性（确定审核范围+维度）→ 步骤2提取与核实（双重读取+内容核实与初筛）→ 步骤3审核判断（法务词汇+广告法+标签合规检查+企标对照）→ 步骤4输出与沉淀（报告+知识库回写）。每步有明确输入/输出/人工确认点，不自动跳步。删除旧的6步流程和独立模式A/B/输出格式章节，精简为参考数据。**步骤职责分离**：步骤2负责内容准确性（数据+错别字+标点+逻辑矛盾+格式统一，宁多勿漏），步骤3纯合规判断（广告法+标签要素完整性+企标） |
| 2026-05-25（v5） | **法务背书词汇来源从 IMA 知识库迁移到企业微信文档**：① 前置条件改为企微 CLI 授权检查（文档权限 + 获取成员文档内容权限）；② 步骤3a 读取方式改为 `wecom-cli doc get_doc_content`（异步轮询）；③ 步骤4b 回写改为 `wecom-cli doc edit_doc_content`（企微文档支持团队共同编辑，彻底解决非所有者无法写入问题）；④ 文档信息更新为《蔡氏福宁 违禁词白名单》URL + docid；⑤ 移除所有 IMA 相关依赖（腾讯ima skill、~/.config/ima/ 凭证） |
| 2026-06-03（v6） | **合规重构 + 外迁瘦身**：① P0修复：步骤4b 企微写入改用 Node.js（解决Shell JSON转义+补全异步轮询）+ references/路径全部改为绝对路径；② P1修复：法务词汇预加载机制（步骤1完成后台异步启动）+ 过滤标准收紧（10字→30字/乱码率30%→15%）；③ **外迁**：打假风险框架、渠道与品类对照、输出模板移至 references/，SKILL.md 从 536 行瘦身至 ≤ 400 行；④ description 增强触发词密度（补 10+ 自然语言触发词） |
